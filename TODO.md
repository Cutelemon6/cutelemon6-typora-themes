# 待审阅格式与发布前 TODO

状态：DRAFT · 等待用户审阅。记录日期：2026-09-19。

当前工作位于 `review/wide-night-code`，基线为 `50c464a`。初版已经发布；本轮按用户要求将宽度、字体、代码样式、预览改动及 TODO 提交并同步至审阅分支。**同步进度不代表完整格式审阅通过；合并主分支和正式发布前仍需完成以下审阅。**

## 已讨论的方向与验证边界

正文与标题的字体、字号、三套主题配色及链接方向已讨论。宽屏编辑区采用可用容器的 75%（包含左右各 32 px 内边距），窗口宽度不超过 1000 px 时铺开。代码采用 Monaco 优先的内置 Night 字体栈；Night 代码底色为 `#27292d`，语法颜色参考内置 Night，行号由 Typora 设置控制。

浏览器已核对三套主题的字体加载、宽窄屏布局和基础代码配色；Typora 新窗口已观察到新版 Night 的宽版心、代码配色和行号。上述检查只证明局部表现，不代表以下格式已经得到用户确认，也不代表 PDF、其他平台或所有编辑状态已验证。

本机 Typora 主题目录已同步本轮 `base.css` 和 Night 样式。旧文档窗口可能缓存旧 CSS；切换主题未能刷新旧窗口，新窗口加载了新版。后续原生审阅应先核对实际加载版本，并保全用户文档后再重开。不要把浏览器预览更新当作已安装主题更新。

## 优先补齐的格式

- [ ] **GitHub 提示框 / Alerts**：加入 NOTE、TIP、IMPORTANT、WARNING、CAUTION 五类样张；逐项确认边线、标题、图标、正文和背景，在 Day、Night、Editorial 下保持清晰。检查 `#write blockquote` 对原生 `.md-alert` 类型边线的覆盖；比较普通引用与提示框，并覆盖提示框内列表、代码、链接及多段落。
- [ ] **YAML Front Matter**：加入文首由 `---` 包围的 title、author、date、tags 及多行字段；独立确认字号、字体、底色、边线、内边距和编辑状态。目前 `pre.md-meta-block` 与代码块共用盒子样式，需要先审阅再决定是否拆分。
- [ ] **目录 `[toc]`**：补齐不同标题层级、长标题换行、缩进、链接、当前项与上下间距；确认正文目录和侧边大纲各自的效果。
- [ ] **完整格式样张**：在同一份 Markdown 中覆盖本清单，配套标注哪些扩展需要 Typora 设置。三套主题使用相同内容，避免只凭浏览器静态样张判断原生效果。

## 其他尚未完整审阅的格式

- [ ] **标题与文字格式**：H1–H6，尤其 H5/H6；中英文粗体、斜体、删除线、`==高亮==`、上下标、`<kbd>`、长链接、引用式链接；编辑时 Markdown 标记的可读性与间距。
- [ ] **列表与任务项**：多级有序/无序混排、多段落列表、列表内引用/代码/表格、长行换行；复选框对齐、已完成与未完成状态。
- [ ] **普通引用与分隔线**：多层引用、多段落引用及引用内块元素；正文独立 `---` 分隔线的粗细与前后留白。文首 YAML 与正文分隔线分别审阅。
- [ ] **表格**：左/中/右对齐、宽表、多行内容、长英文/代码字段、嵌入链接与行内公式、隔行底色、编辑控件、窄窗口表现。
- [ ] **公式**：行内及块公式、长公式、多行对齐、编号、公式编辑状态；检查文字基线、上下间距、深浅主题对比和超宽内容。
- [ ] **脚注**：标记、悬浮内容、定义区、多段落脚注、返回链接与导出表现；现有样张只有简单脚注，尚未完整确认。
- [ ] **图片**：宽图、小图、透明 PNG/SVG、指定宽高、图片链接和失效图片；检查深色背景可读性、对齐和相邻内容间距。
- [ ] **图表**：Mermaid 流程图、时序图等常用图形；检查深浅主题下文字/线条/背景、宽图溢出和导出。其他图表语法按实际使用需求确定范围。
- [ ] **其他嵌入内容**：按实际使用需求确认 HTML 注释、`<details>/<summary>` 折叠内容及其他原生支持的 HTML 元素；不将未测试的元素列为已支持。

## 已讨论格式仍需补充的检查

- [ ] **代码边界情况**：行号开/关、多位数行号、长行自动换行/横向滚动、空代码块、不同语言、复制与选中、代码编辑状态、代码嵌在列表/提示框内；确认底色和 Monaco 的最终观感。
- [ ] **布局与编辑状态**：全屏、有/无侧边栏、窄窗口、缩放；源码模式、聚焦模式、打字机模式、搜索命中、选中、光标及弹出控件。
- [ ] **三主题一致性**：完成 Day、Night、Editorial 的同内容原生审阅，确认已讨论的基础排版没有在扩展格式中产生冲突。
- [ ] **平台边界**：当前只检查 macOS；Windows/Linux 未实测，Monaco 缺失时会回退。若不测试，应继续在文档明确说明。

## 导出与发布门槛

- [ ] **PDF / 打印**：白底转换、标题孤行、段落分页、跨页表格、长代码换行、图片/图表裁切、脚注、提示框及 YAML 导出行为。
- [ ] **HTML 导出**：字体与资源路径、提示框、公式、脚注、图表、宽度和深色样式；区分浏览器预览与 Typora 原生导出的结果。
- [ ] **文档与截图**：本轮样式确定后更新仓库截图和说明；当前仓库静态宣传截图仍代表初版，不能作为本轮修改的验收依据；重建时统一图片扩展名与实际编码（当前 `.png` 文件实际为 JPEG）。
- [ ] **用户 review**：交付完整样张的三主题效果与剩余限制，由用户确认结果。
- [ ] **提交前核对**：保全已有暂存/未暂存改动；核对本任务差异，运行 `python3 scripts/check.py` 与 `git diff --check`，确认字体许可与资源完整性。
- [ ] **合并主分支与正式发布**：满足用户 review 门槛后再执行；审阅分支的同步不构成正式发布或合并授权。

## 下次从哪里继续

先补提示框、YAML 和目录样张，审阅设计后再扩展其他格式。仓库内可使用 `preview/index.html` 和 `examples/showcase.md` 继续审阅。在仓库根目录运行 `python3 -m http.server 8000 --bind 127.0.0.1`，打开 `http://127.0.0.1:8000/preview/`；原生效果需在 Typora 中打开 Markdown 样张核对。

参考：[Typora Markdown 格式](https://support.typora.io/Markdown-Reference/)、[GitHub 风格提示框](https://support.typora.io/What's-New-1.8/)、[代码块设置](https://support.typora.io/Code-Fences/)。
